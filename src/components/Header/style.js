import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    backgroundColor: '#4CAF50',  // Menggunakan warna langsung, contoh hijau
    padding: 16,
    alignItems: 'center',
    marginBottom: 20,
    marginTop: 20,
  },
  title: {
    color: '#FFFFFF',  // Warna putih
    fontSize: 20,
    fontWeight: 'bold',
  },
});
